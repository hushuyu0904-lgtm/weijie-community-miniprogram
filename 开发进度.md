# 开发进度

## 第一阶段：基础导航与活动浏览

日期：2026-09-24。基线：《第一版框架设计 v0.1｜开发交接版》。
状态：代码完成，本地静态与页面逻辑检查通过；微信开发者工具导入、渲染及真机预览待手动验收。

### 本轮交付

- 唯一写入目录：本文件所在的 wechat-miniprogram 目录。
- 微信原生小程序；无新增 npm 依赖，无云资源部署。
- 四个导航：活动、招聘、交流、我的；活动列表与活动详情。
- 加载、空数据、失败重试、无效活动 ID、返回及直接进入详情的处理。
- 招聘、交流、我的显示“尚未开放”，不提供假成功按钮。
- services/activities.js 为唯一活动数据入口，目前仅返回本地演示数据。
- 没有实现真实身份、管理员授权、支付、占位、退款、内容审核或管理功能。
- 原浏览器小样的四个文件未修改，前后 SHA256 一致。

### 导入与预览

1. 打开微信开发者工具，导入本文件所在的 wechat-miniprogram 文件夹（不是内部 miniprogram 文件夹，也不是原 HTML 小样文件夹）。
2. 项目类型选小程序；源码根目录由 project.config.json 指定为 miniprogram/。
3. 当前 appid 为 touristappid，仅供本地演示工程。根据工具实际提供的“测试号／无 AppID”选项导入；如当前版本不接受该方式，换成你有权限的真实小程序 AppID 后编译。AppID 不是密钥，不需要提供 AppSecret。
4. 不启用云开发、不安装 npm 依赖，直接编译。预期首页为“活动”，底部出现四个文字导航。
5. 当前不具备云环境；所有活动、费用和名额均为演示。没有真实库存、登录或交易。
6. 真机预览取决于 AppID、登录账号权限及开发者工具支持情况，本轮未验证。

### 本地检查

在本目录执行：

    node check.cjs

实际执行结果：9 组通过，退出码 0。

覆盖：JSON 配置、JavaScript 语法、页面文件与导航对应、按钮处理函数；演示数据及无效 ID；数据入口空/错场景；列表加载、空、错、重试；列表跳转详情；详情加载、错、重试、不存在；返回和直接进入回退；过期异步响应与页面卸载；未开放页面无模拟身份或按钮操作。

检查以 Node 中的页面逻辑和微信 API 替身完成，不能替代微信 WXML/WXSS 编译、实际点击、页面渲染或权限验收。

### 待手动验收

- 导入编译无报错，四个导航名称正确，文字可读、按钮不被遮挡。
- 两张活动卡片打开各自详情，零剩余名额仍显示为示例，报名仅显示尚未开放。
- 详情返回原列表正常；通过编译启动路径直接打开详情时，“返回活动列表”也正常。
- 编译启动路径 pages/activity-detail/index，参数 id=unknown，以及不带 id 时，显示无效链接提示。
- 临时修改 miniprogram/services/activities.js 的 DEMO_SCENARIO：list 设为 empty 或 error，detail 设为 error，重新编译，检查空与失败状态。
- 将场景恢复 normal 后重新编译确认正常数据。错误恢复后点击重试的逻辑已用本地脚本验证；编译通常会重新创建页面，不冒充同页真实网络恢复。
- 加载提示通过异步状态逻辑验证，本地数据返回很快，实际屏幕上可能仅短暂出现。
- 不开放页面没有登录、管理、发布、支付等假操作；页面持续区分本地演示与真实后端。

手动故障验收完成后，将 DEMO_SCENARIO 恢复为 list: normal、detail: normal。

### 已知限制与下一阶段

- 微信开发者工具常见安装路径未发现 CLI，本轮没有执行开发者工具编译或预览。
- 未接通真实身份，权限验收不通过也不作已通过声明。
- 下一阶段最小任务：确认 AppID 与现有云环境，接入服务端身份及只读活动云函数，替换当前活动数据入口；仍不实现支付。
- 继续保留“小程序发帖评论＋微信群实时聊天”、本人一个名额、待付款占位 10 分钟的后续设计，本阶段不模拟实现这些业务。

## 第二阶段：可信身份、服务端权限与活动发布

日期：2026-09-24。状态：本地代码与检查完成，真实云端闭环尚未验收；缺少 AppID 和 CloudBase 环境，不宣称本阶段已全部完成。

### 本轮确认与修改

- 已确认仅管理员预先加入的成员可浏览；未登记或停用的用户拒绝访问活动。用户确认当前无 AppID、无云环境。
- 新增 cloudfunctions/community 云函数，使用 getWXContext 的可信 OPENID/APPID 获取身份；核对服务端环境变量 WECHAT_APPID。
- members 角色只由环境持有人在控制台受控登记，没有客户端注册/修改角色接口。身份编号由服务端计算，前端仅可复制本人的编号。
- 新增草稿保存、发布、管理列表与详情，服务端校验必要字段、整数金额/容量、时间关系、发布截止时间及多余字段；成员直接调用管理接口被拒绝。
- 草稿仅管理员可读，发布后已登记成员可读；已发布活动本阶段只读。保存/发布采用版本和状态条件更新，不覆盖已发布内容。
- “我的”接入身份查询及管理员入口；新增“活动管理”“活动编辑”，保持米白底和紫色样式。其他管理、支付、订单、退款仍未开放。
- 活动列表/详情统一通过云函数；默认云端模式，未配置或失败时显示错误。第一阶段数据入口原样保留于 services/demo-activities.js，只有显式将 config.js 的 mode 改为 demo 才使用，不会自动回退。
- 云端活动展示人数上限，不把容量假装成真实剩余库存。本阶段尚无报名或占位逻辑。
- 新增 database/deny-client.rules.json，须对 members/activities 两个集合分别应用。该文件当前未在云端生效，不能作为已部署权限验证。
- 新增云环境配置.md，包含受控初始化步骤、索引和规则要求、三个优先验收用例及客户端绕过测试。未部署任何云资源，未发起交易。
- project.config.json 的 appid 仍为 touristappid；miniprogram/config.js 的 envId 为空。无密钥写入代码。

### 实际执行的检查

- node check.cjs：9 组回归检查通过，退出码 0，保留第一阶段配置、导航、演示异常状态和返回逻辑检查。
- node check-cloud.cjs：10 组检查通过，退出码 0。运行实际云函数处理代码，使用内存数据库和微信上下文替身。
- 优先结果：普通成员直接调用管理接口被拒绝；非法字段不写库；草稿成员不可见，管理员发布后成员可读取。
- 额外覆盖：伪造 userInfo 不提权、未登记/停用拒绝、AppID 不匹配拒绝、缺配置拒绝、角色撤销生效、重复保存/发布、并发编辑冲突、过期发布拒绝、分页、后端错误不回退演示、表单时间校验、保存与发布的页面逻辑。
- 使用 npm 查询并固定 wx-server-sdk 为 4.0.2；未执行云端依赖安装或部署验证。查询缓存位于本项目 .npm-cache，已列入 .gitignore。
- 未改写第一阶段记录；其当时的演示导入步骤是历史记录，当前默认模式及配置方式以本节和云环境配置.md 为准。

### 尚未验证／下一步

- 本地检查不能证明微信身份透传、实际数据库拒绝规则、SDK 云端兼容性、索引配置或开发者工具渲染正确。
- 尚需真实 AppID 和绑定的微信云开发环境；环境持有人按云环境配置.md 执行配置及部署，外部环境操作另行授权。
- 使用管理员、已登记成员、未登记用户三种真实账号，验证创建→草稿不可见→发布→成员列表及详情可见，并验证客户端直接数据库读写均拒绝。
- 这些真实检查通过后才可将第二阶段标为完成。本轮停在本地代码和配置材料，不进入支付、订单或第三阶段。

## 新版前端：活动列表与详情布局

日期：2026-09-25。用户确认采用现有配色、活动卡片布局、TDesign 按需组件方向。

- 导航更新为“活动、机会、连接、我的”；沿用 jobs/community 内部路径，避免无必要的路由迁移。机会与连接仍为尚未开放说明页。
- 列表展示标题、简短介绍、时间、地点、费用和详情按钮。详情拆分活动介绍、参与说明，底部固定“报名功能尚未开放”，预留安全区域，未加入假报名操作。
- 演示活动改为免费，演示来源标识保留。零金额显示免费，非零真实金额照实展示并提示首发暂不支持收费报名；不伪造免费、不显示真实剩余名额。
- 两页使用现有原生控件，新增局部样式；尚未安装 TDesign，待有复杂表单需求再按需引入。未复制外部代码或素材。
- 保留真实数据入口、身份与权限逻辑；没有部署、支付、订单或后台修改。旧版服务端仍接受收费活动，需要余同学后续将首发发布规则限制为免费，本轮未宣称解决。
- 本地检查：check.cjs 10 组、check-cloud.cjs 10 组通过；git diff --check 无空白错误。均为本地逻辑/替身检查，不代表真实云端验收。
- 预览：开发者工具导入本目录；当前默认 cloud 且环境为空，会显示配置错误。仅预览布局时可临时将 miniprogram/config.js 的 mode 改为 demo；演示提示不会消失，联调前恢复 cloud。
- 未执行微信开发者工具编译或真机预览。待手动检查长标题、卡片跳转、四个导航、小屏底部遮挡、安全区域、空/错/无效 ID 状态。
- 下一步：双方确定免费报名接口与状态，再开发真实报名；机会、连接、职业画像等新版能力未在本轮实现。以上新版范围替代历史付费首发及发帖评论方向，历史记录保留。

### 彩色首页导入原生小程序（2026-09-25）

- 用户批准将视觉小样导入。活动首页新增青绿渐变、品牌区域、原创插画封面、彩色图标及大日期；详情仅统一局部色彩，原有数据、权限、分页、返回和错误处理保留。
- 底部原生导航加入本地 PNG 图标并按确认改为“活动、资源、连接、我的”。资源页面仍为未开放，未假装接通知识库。
- preview/make-assets.py 生成原创几何插画和图标。全部 18 张 PNG 约 20 KB，无外部图片请求。
- 类别仅演示模式筛选；云端尚无分类字段，点击提示待后端接入，全部活动仍完整显示。真实日期取服务端时间，演示日期保持待定。没有报名、支付或后台改动。
- check.cjs 11 组通过；check-cloud.cjs 在进程内覆盖为默认 cloud/空环境后 10 组通过，未改写本机 demo 配置。首次直接运行后端检查因本机 demo 设置在配置断言处停止，非后端业务失败。
- git diff --check 通过。未执行微信开发者工具编译及真机验收；请重新编译并重新手机预览，检查图片、渐变、导航、详情跳转、小屏文字换行。
- 用户本机 miniprogram/config.js 与 project.config.json 改动保留且不提交。后续最小任务为真实活动封面、分类字段与免费报名接口联调。

### HTML 验收入口（2026-09-25）

- 用户要求直接通过 HTML 验收，新增 preview/index.html，双击即可离线打开；没有外部依赖或真实请求。
- preview/build.cjs 从现有 WXSS 与演示数据生成快照，页面模板位于 preview/template.html；布局修改后执行 node preview/build.cjs 更新，HTML 不作为小程序运行代码。
- 可点击活动详情、返回及四个导航，提供加载、空、失败、无权限、无效链接场景；重试仅模拟恢复数据，不模拟报名或管理员成功。
- node preview/check.cjs 通过路由、状态及重试逻辑检查，使用 DOM 替身；没有自动验证浏览器渲染或微信真机。已调用系统默认浏览器打开验收页。

### 视觉统一目标与首页样板（2026-09-26，待确认）

- 用户要求以粗门为参照降低模板感；建立视觉统一目标，先首页及导航样板，再经确认应用现有其他页面。品牌文案固定为“未界｜相似的背景，不一样的故事。”。
- preview/refine/index.html 提供320/390宽度新旧对照；新版采用清晰摄影、TDesign官方图标、深色正文与分层字号，缩短首屏、精简重复封面文字、统一圆角及彩色分类。许可证及照片来源留存于样板子目录。
- Edge 实际渲染正常、加载、空、失败、无权限、超长标题、图片失败；两种宽度无横向溢出，正常图片加载、分类空态、重试恢复、导航触控区检查通过。截图检查仍仅代表浏览器样板。
- 未修改原生页面、后端、权限、AppID或本机demo配置。未执行微信开发者工具及真机验收，未推送本轮未批准样板。整体目标尚未完成；等待用户确认视觉样板后迁移原生并同步GitHub。

### 全页面重新设计（2026-09-26）

- 用户指出字体节奏、信息密度和配图关联不足，并明确要求同时修整其他页面；据此推进原生四tab及详情，替代先前仅首页样板的实施范围。固定品牌文案继续保留。
- 活动采用双专题主视觉与紧凑分类；医疗AI改为主题对应的原创文字海报，删除植物配图方案。资源页以已确认内容作方向预览，连接页展示三步申请说明，我的页面分组展示身份及参与状态。资源/申请/报名等仍未开放，不增加实际业务接口。
- 图标采用TDesign官方SVG转PNG，保留MIT许可证；咖啡摄影保留来源和主题配图标识。新增原生自定义tabBar与安全区留白，导航失败保留原选中项。真实无封面活动显示中性缺省，不按标题猜测内容。
- node check.cjs 11组通过；node check-navigation.cjs通过；check-cloud.cjs以进程内cloud/空环境覆盖运行10组通过。没有改写本机config。原有未开放页检查增加了只允许导航选中同步的验证。
- preview/design/index.html由原生WXML/WXSS生成六页预览；Edge实际渲染14种页面/状态×两种宽度，28项检查通过。修正预览程序对.page选择器的误转换，核对正常/小屏/长标题/缺图和底部截图。报告与截图随预览交接。
- 未执行微信开发者工具编译、真机自定义tabBar/字体/安全区验证。后端、权限、报名规则及本机AppID/环境设置未改。视觉目标仍待用户实看确认，未声称整个产品或真实云端完成。

### 大模型封面选图与本地对照（2026-09-26）

- 用户排除医院、药物设计、分子结构及新增生成图，最终选择“明亮白色桌面与ChatGPT界面”候选第1张。选图固定，不再擅自换主题或调色。
- preview/design/selected-llm/index.html 提供活动首页、详情与资源页的本地排版对照，保留现有文案、演示说明及未开放状态。来源与未确认商用许可记录在同目录README中；图片未接入原生页面，也未作为已授权素材发布或上传。
- Edge浏览器检查3页×320/390两种宽度，无横向溢出、图片正常加载；目视发现并修正列表封面未撑满的问题。浏览器截图不代表微信开发者工具或真机通过。
- 原生小程序、后端、权限及本机环境设置未改；被否定的生成图不作为正式素材。下一步仅在素材许可明确后将已选图片接入原生，随后检查真机裁切。

### 已确认的大模型生成配图接入（2026-09-26）

- 用户后续同意从零生成图片，并批准加入品牌标语、已确认活动主题、彩色图标、笔记和水杯的最终版。本次以该确认替代上一节的第三方候选方案；未对第三方原图换屏或复制其构图。
- 将最终生成图转换为llm-exchange.jpg，接入活动首页专题、对应演示活动列表及详情、资源页。保留图片外活动标题和正文，图片内概念内容不视为已实现的聊天功能。页面明确标注“AI 生成主题配图”。
- 删除被替代的蓝色文字海报样式，新增封面失败文字提示。真实云端无封面活动仍显示中性缺省；后端、权限、报名、四tab及AppID/本机配置未修改。
- 本轮执行check.cjs，11组通过；check-navigation.cjs通过，包括新专题封面失败处理。Edge渲染16种页面/状态×320/390两种宽度共32项通过，目视核对活动小屏、缺图、资源和详情。git diff --check通过。未重复后端替身检查，未执行微信开发者工具编译或真机验收。
- 当前验收入口为preview/design/index.html；此前selected-llm仅为未发布的历史本地候选，未上传第三方候选或被否定的生成图。素材来源与最终编辑提示词随正式配图记录。
- 下一步：微信开发者工具重新编译、手机重新预览，检查实际字体、裁切和底部安全区域。

### 活动、资源、连接三页局部样板（2026-09-26）

- 根据用户确认，新增 preview/review-layout/index.html 浏览器样板，支持同尺寸修改前后对照。原生小程序、后端、权限、AppID 和本机设置未改；样板待视觉确认后接入。
- 活动分类移至“近期活动”下方，左对齐单行横滑，保留原有分类名称和演示筛选。资源使用用户知识库截图并支持放大；连接使用 Pexels 的真实交谈照片，明确标注图库主题配图，保留未开放和三步流程。
- 机会加入辉瑞中国官方职业入口，不宣称管培生在招；资讯加入英矽智能2025-03-13企业融资公告，标注往期。wl0327.cn仅参考首页结构，归属未确认，未复制内容或数据。来源见样板README。
- 运行 preview/review-layout/check.cjs：三页×320/390共6项通过，检查无横向溢出、图片加载、44px以上导航触控、分类筛选/空态/恢复、来源链接结构、截图放大入口、连接未开放、长标题和图片失败提示。截图目视核对首页小屏、资源封面及下半页、连接照片与流程。外链内容已通过资料读取核实，未声称自动点击完整外部网站流程通过。
- 未验证微信开发者工具、真机、公众号文章跳转和云端接口。样板未推送 GitHub，不包含之前未授权图片候选；确认后再迁移原生并同步协作分支。

### 资源分类与人工匹配申请样板（2026-09-26）

- 用户确认首版运营人工匹配；兴趣测评暂无内容；工具推荐仅先放nature.skill。仅增量修改preview/review-layout样板，没有改动原生、后端或环境配置。
- 资源改为机会、资讯、知识库、活动回顾四分类，默认机会；知识库标题缩小；nature.skill放入工具与实践，标注来源待补及未开放。
- 连接照片降低高度，增加申请表体验和职业兴趣探索占位。表单包含已确认基本信息、经历、感兴趣方向与交流问题。只检查填写，不持久保存、不上传、不提交、不产生匹配结果；简历上传禁用，正式接入后支持与文字介绍二选一。
- check.cjs六个页面/宽度组合通过，新增验证分类切换、表单必填拒绝、填写检查不冒充提交、收起保留本页输入、展开无溢出；目视核对320资源首屏、390连接及知识库、320表单。git diff --check通过。未验证微信开发者工具、真机、真实提交及简历访问权限。

### 减少模板感的局部视觉精修（2026-09-26）

- 用户要求先给有效prompt并按其修改。本轮prompt记录在preview/review-layout/README.md；新增polish.css局部覆盖，不修改已确认内容、交互和业务。原生页面及后端未改。
- 资源分类改文字下划线导航，撤去工具与测评卡片的装饰图标、浅底和外框；知识库取消外层卡片；连接主按钮、表单输入边界保留，删除辅助说明套框；底部导航功能图标保留。
- compare.html提供本轮修改前后同尺寸可点击对比。check.cjs六组通过，包括分类、表单、两种宽度、图片失败和长标题检查。实际查看资源320首屏、知识库390、连接390及表单320截图；未验证原生与真机，未推送GitHub。用户确认后再迁移原生。

- 用户随后否定本轮视觉精修，要求恢复上一版：已取消polish.css加载，恢复彩色图标、分类按钮和卡片样式。资源四分类、连接申请表及此前已确认功能保留。当前使用preview/review-layout/index.html；原生代码未改。

### 用户提供图片的实际试排（2026-09-26）

- 按用户授权比较新旧图片，仅在浏览器样板修改素材。活动Coffee Chat保留原图库人物照片，新咖啡笔记本图保留宽幅备用；连接采用用户提供的生成交流图，标注AI生成/非现场，改为3:2避免裁掉头部与手势。绿色对话插画缩至54×40显示窗口，在连接说明旁仅出现一次。
- image-review.html提供原图/新图试排对照；index.html显示选定组合。保留原样式和全部功能，不生成新图、不修改后端或原生，不推送素材。用户提供素材的商用许可未独立核实。

- 用户随后选择改图前版本：默认样板恢复原图库交谈照片和原裁切，撤去绿色气泡插画；新素材仅保留候选对照，其他页面、内容、表单及原生保持不变。

### 确认版同步原生与协作分支（2026-09-26）

- 用户授权同步GitHub及微信预览。已将活动分类位置、资源四分类、知识库截图、nature.skill占位、原图库连接照片和本地申请表落到原生页面；保留彩色图标和卡片，不使用已否定的polish样式及新生成素材。
- 表单仅内存填写和校验，页面卸载清空，无上传、持久化或真实提交；简历、匹配和测评未开放。普通外部网页采用“复制来源链接”，有复制失败提示，未假装接通公众号或web-view。
- 执行check.cjs：11组通过；check-navigation.cjs、check-resources.cjs通过。原生源码生成20种浏览器页面/状态×320/390共40项通过，实际查看活动小屏、知识库、连接与展开表单截图。图库来源、README及新逻辑检查已同步。
- 微信开发者工具CLI预览已尝试，返回服务端口关闭，未生成新预览码；未改本机安全设置、AppID及云配置。需手动编译/预览或开启服务端口后再试。浏览器与替身检查不代表微信编译或真机通过。
