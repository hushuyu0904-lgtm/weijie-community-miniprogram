# 未界社群小程序

微信原生小程序 + 微信云开发 CloudBase。仅包含社群小程序代码，不包含外层医学职业简历项目资料。

当前状态：基础导航、活动浏览、服务端身份与权限、管理员保存草稿和发布活动的本地代码已完成。9 组页面回归和 10 组后端/页面逻辑检查通过。尚无真实 AppID 和云环境，未部署、未完成真实身份、数据库规则及双账号验收。

## 两人当前分工

| 负责人 | 现在做什么 | 交接结果 |
| --- | --- | --- |
| 余同学：小程序账号与环境 | 准备小程序账号和 AppID，确认主体及开发权限；准备对应微信云开发环境；按配置文档执行获授权的环境操作 | AppID、环境 ID、部署/数据库规则状态；不在仓库或聊天中传密钥 |
| 书玉：业务确认与代码协调 | 保持设计范围；准备一条测试活动；协调代码修改、检查及合并；记录问题 | 测试活动内容、可复现问题、可交接代码版本 |
| 两人共同 | 一个账号为管理员，一个为成员；再将成员临时停用测试拒绝访问 | 创建草稿→发布→成员可读；普通成员管理操作被拒绝；未获准用户不可浏览 |

代码修改可继续由 Codex 协助。余同学是否负责写代码尚未确认，当前不把后端开发强行分给他。

## 导入与检查

用微信开发者工具导入本仓库根目录；源码目录由 project.config.json 指向 miniprogram/。

当前默认云端模式，环境未配置时会明确报错，不自动返回演示数据。仅查看第一阶段演示时，可临时将 miniprogram/config.js 的 mode 改为 demo；验收后恢复 cloud，演示模式没有真实身份或管理功能。

在仓库根目录执行：

```sh
node check.cjs
node check-cloud.cjs
```

这些是本地逻辑检查，不是微信开发者工具渲染或真实云端验收。

- [开发进度](开发进度.md)：按阶段追加结果与未验证项。
- [云环境配置](云环境配置.md)：集合规则、管理员初始化、部署与真实验收步骤。

## 最简单的协作约定

1. main 保存已检查、可交接的版本；当前基线仍未通过真实云端验收。
2. 每次只处理一个任务，用短期分支提交；不要两个人同时修改同一文件。
3. 合并请求写明“修改内容、实际检查、未验证事项”；另一人复核后合并。不要求非开发者凭感觉评审权限代码，权限必须有测试与真实账号验收依据。
4. 不提交真实成员资料、数据库导出、密钥、凭据、node_modules 或本地工具配置。账号授权通过平台权限管理，不共享密码。
5. GitHub 协作权限、小程序开发权限、CloudBase 环境权限是三套不同权限，分别授予。持有仓库权限不等于获得云资源管理权限。
6. 当前不配置自动部署、不发布、不接入支付或订单；外部环境操作另行确认授权。

## 下一步只完成这条链路

余同学准备账号和环境 → 获授权后配置规则与部署 → 两人做真实账号验收 → 记录问题并修复。通过后再启动下一阶段功能。
