# 未界社群小程序｜两人协作交接

交接日期：2026-09-24。代码盘点基线：`7aa30cc`。书玉负责前端与业务验收，余同学负责后端与云环境。

仓库：https://github.com/hushuyu0904-lgtm/weijie-community-miniprogram （私有，书玉添加余同学访问权限后可见）。

## 1. 先了解我们在做什么

面向社群成员的微信小程序：运营方发布收费活动，成员报名；成员发布招聘并评论；社区采用“小程序发帖评论＋微信群实时聊天”。

已经确认的边界：一人购买一个活动名额、待付款占位 10 分钟；不增加小程序即时通信、商城、分销、多票种或复杂会员体系。这些是后续实现基线，当前代码还没有支付、订单或占位功能。

**回家后这一轮只跑通：管理员创建活动→保存草稿→发布→已获准成员在列表看到并打开详情。** 不同时启动招聘、讨论或支付开发。

## 2. 当前代码已有什么

| 部分 | 已有实现 | 实际验证程度 |
| --- | --- | --- |
| 前端 | 活动、招聘、交流、我的四个导航；活动列表、详情；身份查询；活动管理与编辑 | 本地页面逻辑检查通过，开发者工具渲染和真机仍待验证 |
| 后端 | 一个 community 云函数；可信身份、成员/管理员权限、字段校验、草稿保存、发布及查询 | 本地替身测试通过，未部署到真实环境 |
| 数据库 | members、activities 的使用代码及拒绝客户端直读写的规则文件 | 集合、索引和规则尚未在真实环境配置 |
| 未开放 | 招聘、交流、报名、订单、退款及其他管理功能 | 不提供假成功操作 |
| 检查 | check.cjs 9 组，check-cloud.cjs 10 组 | 此前均通过；不是微信真实身份或云端权限验收 |

当前 AppID 是 `touristappid`，环境 ID 为空，默认模式是 `cloud`。因此看到“尚未配置环境”是当前真实状态，不应通过模拟登录或模拟管理员绕过。

已有代码是起点，不要求余同学重写后端。发现技术冲突时，记录具体问题和最小修复，不重新设计整个产品。

## 3. 书玉回家后的任务：前端

按顺序做：

1. 将余同学加入私有仓库；用微信开发者工具导入仓库根目录。
2. 创建自己的任务分支，例如 `frontend/activity-ui`，保留现有米白底、紫色和卡片风格。
3. 环境尚未准备好时，可临时将 `miniprogram/config.js` 的 mode 改为 `demo`，检查活动列表、详情、返回、导航和小屏显示。演示只覆盖成员活动浏览，不能验证真实管理页面。
4. 对实际发现的问题做局部修复：文字遮挡、表单不好填写、错误提示不明确、失败丢失输入等。没有问题就不为改而改。
5. 后端可用后，恢复 `cloud` 并接入环境，检查“我的→活动管理→新建→保存→发布”页面流程。

**前端交付物：** 一个可复核的分支/合并请求，说明修改和实际检查；以及未解决问题清单。临时 demo 配置不要作为默认云端配置合入 main。

主要修改：`miniprogram/`，以及相关页面检查。`miniprogram/services/` 由书玉维护；接口变化须先与余同学对齐。可继续让 Codex 协助代码与检查。

## 4. 余同学回家后的任务：后端与环境

按顺序做：

1. 接受仓库邀请，克隆仓库，创建任务分支，例如 `backend/cloud-setup`。先读本交接，再读 [云环境配置](云环境配置.md)，不用全仓库阅读。
2. 运行两个本地检查脚本，定向检查 `cloudfunctions/community/index.js` 的身份来源、权限、输入校验和状态更新，发现问题再局部修复。
3. 准备真实小程序 AppID、对应微信云开发环境及双方需要的开发权限。环境创建、费用及部署权限先与书玉确认；不共享账号密码或密钥。
4. 按配置文档建立 members、activities，保存拒绝客户端直读写的规则和所需索引，再配置 WECHAT_APPID、部署 community 云函数。规则文件放在仓库里不等于已经生效。
5. 通过真实小程序取得本人身份编号，由环境持有人核实后在控制台受控建立管理员和成员记录。没有自助注册管理员接口，不以请求里的 OPENID/role 作为授权依据。
6. 优先验证：成员直调管理接口被拒绝；非法金额/容量/时间/多余字段被拒绝；草稿不可见、发布后可读；客户端直接数据库读写被拒绝。

**后端交付物：** 可复核的代码分支/合并请求，以及 AppID、环境 ID、部署所用提交号、规则/索引配置状态、真实检查结果和待解决问题。AppID/环境 ID 可以交接，密钥和真实成员编号不写进公开记录或提交。

主要修改：`cloudfunctions/`、`database/`、`check-cloud.cjs` 和环境配置文档。前端缺信息时先提供接口样例，不直接改动书玉正在编辑的页面。

## 5. 双方交接的接口，以现有代码为准

调用同一个云函数 `community`，通过 action 区分操作：

| action | 参数（不含 action 本身） | 谁可用 |
| --- | --- | --- |
| identity | 无 | 能验证微信身份的用户；返回本人的 memberKey、role、canBrowse |
| listActivities | offset，可省略 | 已登记且有效的成员/管理员；只返回已发布活动 |
| getActivity | id | 已登记且有效的成员/管理员；草稿或不存在活动返回 null |
| listManagedActivities | offset，可省略 | 管理员 |
| getManagedActivity | id | 管理员 |
| saveDraft | id、version、activity | 管理员 |
| publishActivity | id、version | 管理员 |

activity 字段：title、description、location、priceFen、capacity、startAt、endAt、deadlineAt、refundPolicy。金额使用整数分；时间使用毫秒时间戳，前端按北京时间展示。草稿新建 version 为 0，后续使用服务端返回的 version；不要前端自行猜版本。

云函数成功返回 `{ ok: true, data }`，失败返回 `{ ok: false, code, message }`。现有错误码：NOT_CONFIGURED、UNAUTHENTICATED、FORBIDDEN、INVALID_ARGUMENT、NOT_FOUND、CONFLICT、BACKEND_ERROR。前端 services/cloud.js 已处理这层返回。

需要修改接口时，在合并请求或双方沟通中写清“旧字段→新字段、原因、影响页面”。默认保持这些约定，避免两边各自修改。

## 6. 如何各做各的、不互相卡住

- 书玉检查和修复前端，余同学同时检查后端、准备环境；谁完成一项就提交明确结果，不必等待全部做完。
- 公共配置 `project.config.json`、`miniprogram/config.js` 由书玉负责合并填写；余同学提供对应值，部署前以配置文档核对。余同学本地验证需要的临时配置不要覆盖对方改动。
- 每人一个任务分支，经合并请求合入 main；不把前后端开发分成长期互不合并的两套项目。
- 不同时改同一文件；另一人的必要改动通过小提交协调。
- GitHub、微信小程序、云环境权限分别管理；有仓库权限不代表有云环境权限。
- 工作结束时只留四项：完成什么、提交号、实际验证什么、还需要对方提供什么。不要把“测试替身通过”写成“真实接口已通”。

## 7. 下一次共同验收与停止条件

可以远程进行：两人分别用自己的微信身份操作，不必等下次见面。初次可让余同学使用管理员账号、书玉使用成员账号，具体以双方登记结果为准。

- [ ] 管理员创建完整活动并保存草稿，成员列表和直接详情调用均看不到草稿。
- [ ] 管理员发布后，成员重新进入列表能看到，并能打开正确详情和返回。
- [ ] 成员直接调用管理接口被拒绝；在前端伪造管理员标记不生效。
- [ ] 非法金额、容量、时间或多余字段被拒绝，数据库不产生错误记录。
- [ ] 客户端直接读取/写入 members、activities 均被规则拒绝，包括管理员客户端。
- [ ] 临时停用成员后，其活动请求被拒绝；完成后由受控操作人员恢复资格。
- [ ] 断网或云函数失败时，页面明确提示失败，不自动展示演示数据或假成功。

代码检查、页面可用性和真实账号权限检查都通过，才记录本阶段完成。若账号或环境仍受阻，分别提交已完成的前端/后端工作并记录阻塞，暂不进入支付或新业务开发。

项目完整历史见 [开发进度](开发进度.md)。本文件只是异步分工交接，不替代已确认设计，也不新增功能范围。
