# 第二阶段云环境配置与真实验收

当前状态：没有真实 AppID 或 CloudBase 环境。文件只提供部署材料，未创建集合、应用规则、部署云函数或执行真实身份验收。

## 前置条件与配置

使用绑定该小程序的“微信·云开发”环境和文档型数据库。本项目使用 wx.cloud + wx-server-sdk，不混用 Web 匿名登录或 CloudBase 自定义登录。

1. 准备真实小程序 AppID、微信云开发环境 ID，以及有权管理该环境的微信账号。只需 AppID/环境 ID，不向聊天、前端或代码写入 AppSecret、SecretId 或 SecretKey。
2. 将 project.config.json 的 appid 从 touristappid 改为真实值；miniprogram/config.js 保持 mode: 'cloud'，填写 envId。
3. 由环境持有人在云数据库新建 members、activities 两个集合。不要使用已有业务集合。
4. 对两个集合分别设置“仅管理端可读写”，或使用 database/deny-client.rules.json 的完整内容作为该集合的自定义规则：read: false、write: false。包括小程序管理员在内的所有客户端都不能直读、创建、更新、删除；云函数依服务端权限访问。仓库中的规则文件不会自动生效，必须在控制台保存。
5. 在 activities 创建配合列表查询的索引：status 升序、createdAt 降序、_id 降序；另一个索引 createdAt 降序、_id 降序用于管理列表。如控制台已有覆盖索引则复用，不重复创建。
6. 上传 cloudfunctions/community，选择云端安装依赖。依赖已固定 wx-server-sdk 4.0.2；选择平台支持且兼容该版本依赖的 Node.js 运行时，并在部署后核查日志。当前尚未做云端兼容性验证。
7. 配置该云函数的环境变量 WECHAT_APPID，值须与小程序真实 AppID 完全相同。没有此变量、没有可信 OPENID 或 AppID 不匹配，函数拒绝请求。不要开启 HTTP 公网触发器，不向普通成员授予云控制台或服务端密钥权限。
8. 开发者工具重新编译。进入“我的”发起真实身份查询；未登记的人只能得到本人的身份编号和未获准提示，不能读取活动。

创建云环境、集合、索引，应用规则、设置环境变量和部署函数均为外部环境操作，需另行授权后执行。未完成步骤 4 的规则保存与实际验证前，不导入真实成员或活动资料。

## 管理员和成员的受控登记

没有“注册管理员”或“第一个登录者自动成为管理员”的接口。

1. 拟任管理员本人在已配置小程序的“我的”页取得身份编号，交给环境持有人。该编号由服务端以 AppID 与可信 OPENID 计算，不接受客户端自报 OpenID。
2. 环境持有人通过可信沟通核实本人及授权范围后，在 members 手动建立一条文档：

   - `_id`：本人“我的”页复制的 64 位身份编号。
   - `role`：字符串 `admin`。
   - `active`：布尔值 `true`，不是字符串。

3. 管理员重新验证身份，才会出现“活动管理”。服务器仍在每个管理请求重新检查角色，前端显示按钮不构成授权。
4. 普通成员同样先查询本人编号，管理员核实同意后由具有环境权限的受控操作人员写入 members，role 为 `member`、active 为 true。首版不另做成员管理后台，不向普通成员提供写入角色的方法。
5. 停用时将 active 改为 false；下次活动或管理请求即被拒绝。修改成员表属于受控管理操作，应使用控制台账号并保留平台审计记录。

身份编号不是密码或访问令牌，知道编号不能在云函数中冒充对方；但不能凭未经核实的转发编号给陌生人授予成员或管理员资格。

## 本轮业务约束

- 仅 active: true 且 role 为 member/admin 的预登记用户可以浏览已发布活动。
- 草稿仅管理员通过管理接口读取；成员详情接口对草稿和不存在活动均返回空，不泄露内容。
- 所有管理接口先校验服务端角色。请求中的 role、OPENID 等多余字段被拒绝；即使平台附加 userInfo，也不据此授权。
- 必填标题/说明/地点/退款规则的最大长度分别为 120/5000/200/2000 字符；金额为非负安全整数分，容量为正安全整数。限制只用于输入校验，不意味着支持任意大金额的微信支付。
- 时间为有效毫秒时间戳：截止不得晚于开始，结束必须晚于开始；发布时截止必须仍在未来。前端按北京时间输入并校验真实日期。
- 已发布活动本阶段只读。草稿保存使用稳定编号与版本检查，重复保存和并发编辑不会覆盖已发布记录；发布是条件更新，重复确认不重复生成活动。
- “退款规则：待确认”可用于本阶段活动介绍，因为报名收款仍关闭；未来支付开放前必须核实规则，不能照搬为收款条件。
- 每次最多读取 20 条，支持加载更多；本阶段 offset 上限 10000，超过需调整分页实现，避免无限读库。
- 目前只提供创建、保存草稿、发布、查看，不实现取消活动、成员报名、订单、支付或退款。
- 默认云端模式。任何连接失败或无权限都不回退演示数据。只有开发者显式把 config.js 的 mode 改为 demo 才保留第一阶段演示；该模式不能使用身份与管理功能。

## 三个优先真实验收用例

准备管理员 A、已登记成员 B、未登记用户 C，均使用各自微信身份进入小程序。不要把云控制台运行测试（缺少微信上下文）当作身份透传验证。

1. 越权拒绝：B 在小程序调试器中直接调用 community 的 listManagedActivities 或 saveDraft/publishActivity，必须返回 ok:false、FORBIDDEN；在请求 userInfo 写入 A 的信息仍然拒绝。C 调用 listActivities/getActivity 也必须被拒绝。A 被停用后同样拒绝。
2. 非法字段拒绝：A 调用 saveDraft，把 priceFen 改为 -1、1.5 或字符串，把 capacity 改为 0，或令截止晚于开始、结束早于开始；分别返回 INVALID_ARGUMENT，数据库不增加/修改活动。额外传 activity.status 或 activity.createdBy 也必须拒绝。
3. 发布后读取：A 在“我的→活动管理→新建活动”保存完整草稿。B 列表看不到草稿，直接调用详情也得不到草稿。A 发布后，B 重新进入活动列表，能打开对应详情；C 仍不能读取。退款/报名按钮仍未开放。

## 必做：验证不能绕过云函数

在仅有测试数据的环境，使用 B/C 的小程序客户端 SDK 直接调用数据库，而不是服务端 SDK。分别验证：

- members：查询、创建一条包含 role:admin 的测试文档、更新自身角色、删除测试文档均被权限拒绝。
- activities：查询、创建、更新、删除测试文档均被权限拒绝。
- A 的客户端直接数据库访问也应被拒绝；A 必须通过 cloud.callFunction 调用管理接口。

写入测试仅使用专门的测试文档，不删除真实记录。若任一直接读写成功，停止使用该环境并修正规则；本地 check-cloud.cjs 无法证明这些云端规则已经应用。

## 本地检查与限制

在项目目录运行 node check.cjs、node check-cloud.cjs。前者回归导航和演示页面，后者运行实际云函数处理代码并使用内存数据库/微信上下文替身，覆盖拒绝、校验和发布流程。不会连接云服务或发起交易。

仍须实际验证：云函数依赖及运行时、索引和安全规则、微信身份上下文、客户端请求、开发者工具渲染和跨账号流程。

## 官方依据

- 微信云函数与可信身份：https://docs.cloudbase.net/recipes/add-cloud-function-wechat-miniprogram
- 数据库安全规则：https://docs.cloudbase.net/database/security-rules
- 腾讯云数据库安全规则说明：https://cloud.tencent.com/document/product/876/41802

本次依据官方检索结果核对 getWXContext 与规则能力；部分文档直读超时，不据此宣称已验证部署兼容性。
